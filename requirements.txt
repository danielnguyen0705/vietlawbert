# ==========================================
# 1. CRAWLING & WEB SCRAPING (Lấy dữ liệu)
# ==========================================
scrapy
requests
beautifulsoup4

# ==========================================
# 2. DATA PROCESSING & UTILS (Xử lý dữ liệu)
# ==========================================
python-dotenv
html2text
cssselect
lxml

# ==========================================
# 3. DATABASES (Bộ 3 quyền lực)
# ==========================================
pymilvus      # Vector DB (Thay thế hoàn toàn cho faiss-cpu)
neo4j         # Graph DB (Lưu trữ Knowledge Graph - Entities & Relationships)

# ==========================================
# 5. AI, LLM & EMBEDDINGS (Core VietLawBERT)
# ==========================================
# PyTorch CPU-only build
--extra-index-url https://download.pytorch.org/whl/cpu
torch
rank_bm25                    # Lexical Filtering cho GG-SLM

transformers
sentence-transformers # Dùng để chạy model BGE-M3 hoặc VNLawBERT
openai              # OpenAI-compatible API cho Groq/GitHub Models
langchain-text-splitters  # Chunking cho contextualizer


# ==========================================
# 7. WEB FRAMEWORK (API & Chatbot)
# ==========================================
chainlit         # Giao diện chat chuyên dụng cho LLM (Thay thế Streamlit)
